# RECOVER 内部边界

- `contracts.ts`：恢复依赖、结构化裁决和最小提示契约。
- `policy.ts`：结构化失败识别、目标阶段选择和修订计划规范化。
- `model-call.ts`：有界 Context 组装、恢复裁决请求和重复文案改写。

`../recover.ts` 只负责重试、升级、终止和状态转移。结构化输出失败只能触发局部重试或用户决策，不能被解释为整个模型或 LS 的能力损坏。
