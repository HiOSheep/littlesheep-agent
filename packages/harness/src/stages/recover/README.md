# RECOVER 内部边界

- `policy.ts`：Runtime 自有的恢复策略——结构化失败识别、未结算副作用判断、失败种类读取和目标阶段选择。

`../recover.ts` 不再请求恢复模型：决策只由记录到的事实推导（可重试 → 回到失败阶段，权限拒绝 → `ASK_USER`，副作用未结算或被中止 → 显式停止），重试受 `maxRecoveryAttempts` 约束，也没有“修订计划”或模型撰写的用户文案。用户可见文字仍只来自 `ASK_USER` 的真实模型调用或 Runtime 状态。
