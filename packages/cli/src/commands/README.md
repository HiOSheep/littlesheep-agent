# CLI Commands

这里保存 CLI 的管理型子命令 adapter。

## 边界

- `memory.ts`：记忆回滚和经验维护入口。
- 旧 `memory archive` 写入命令已经退役；CLI 只保留默认拒绝识别，避免被误当作聊天输入。
- `import-repo.ts`：把仓库知识蒸馏为受控经验的命令。
- 命令只负责参数、进度、退出码和调用领域服务，不复制领域算法。

## 测试与修改定位

- 每个命令的测试与实现同目录。
- 新命令同时更新根帮助、参数解析、错误输出和数据安全说明。
