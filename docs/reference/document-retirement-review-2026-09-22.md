# 文档退役待审清单 2026-09-22

最后更新：2026-09-22 22:20:00

本文件列出**我不能单独决定**的文档处置：它们既不是明显失效，也不能确认已经完成。每条给出证据、我的建议与不做的代价；你批准后我按建议执行（退役 = 先把仍然成立的事实汇总到拥有它的常驻文档，再 `git rm`，原文留在 git 历史里）。

已按同一规则**直接退役**的文档（自查证据充分，不必审批）：
`lean-v2-cache-95-plan-taskbook-2026-09-20`（缓存目标已由真实长任务任务书接续，历史口径已从验收规程移除）、`harness-lean-audit-taskbook-2026-09-12`（审计完成；其开放项 HL-11 双驱动收敛已于 2026-09-21 完成并记入 AGENTS.md，配对性能与发布门由真实长任务专项接续）、`harness-lean-phase-a/-b/-c-implementation-taskbook`（前两批交付完成；第三批方向最终以极简重建落地——DECIDE/CAPTURE/自动演化已删除，原文引用的 `stages/evolve.ts`、`stages/capture.ts` 已不存在）、`harness-rebuild-and-cache-taskbook-2026-09-02`（durable event/inbox/lease/settlement/recovery 已落地并在项目状态中记录）、`agent-runtime-efficiency-versioning-taskbook-2026-07-17`（自述已完成，并注明最新源码状态以项目状态为准）、`reference/harness-rollout-readiness-2026-09-11`（原文自标"已被单一驱动取代，不再描述当前行为"）。

---

## 1. 对话任务连续性（P0）任务书 2026-08-13 — 待你决定

- **证据**：开头写"已立项，P0 阻断；故障诊断与任务书已完成，运行时代码修复尚未开始"（2026-08-13）；但此后状态机重设计已完成 P5（`execute` 可提出澄清）、P2（恢复/重试语义）、P3（VERIFY）等项，`docs/README.md` 仍标"仍未完成"。也就是说：**当时列的五层连续性验收是否已在重设计中关闭，我没有独立证据**。
- **建议**：要么（a）由我逐条对照现状更新状态并关闭 → 退役；要么（b）保留并在文档头补一句"状态已过期，待重新评估"。
- **不做的代价**：一个 2026-08 的 P0 阻断项继续挂在索引上，读者无法判断它是否仍是当前风险。

## 2. LS 状态机重设计任务书 2026-09-18 — 一处未闭环

- **证据**：P1–P5、P7 都标了 ✅；**P6（压缩／检查点续跑／工作策略升级／记忆写入）没有完成标记**。而其中压缩的语义此后已改变（改为真实压力触发；记忆写入只经压缩路径，且在"窗口远大于会话"的配置下不再发生）。
- **建议**：把 P6 拆成"已由后续决策覆盖"与"仍然要做"两部分，前者写进项目状态后关闭，后者转成新任务书或并入真实长任务专项，然后退役本文。
- **不做的代价**：P6 里的记忆写入要求在**当前实现下不会被执行**，留着会让人以为它在运行。

## 3. 开发反馈环提速任务书 2026-08-09 — 阶段 5 长期暂停

- **证据**：状态写"进行中但阶段 5 已暂停"（2026-08-10 最后更新），此后开发反馈环的稳定部分（affected 选择器、`verify:changed/core/full` 三级门、构建新鲜度凭证）已经落地并被 `repository-guide.md`、项目状态收录。
- **建议**：若阶段 5（任务级内循环提速）仍要做 → 保留并补状态；若不再做 → 退役。
- **不做的代价**：一个 8 月的暂停项长期占任务书预算（目录预算 16，现有 8）。

## 4. OpenCode VS Code 对标记录 2026-08-13 — 依据已失效

- **证据**：文档引用的 10 个 LS 源码路径**全部不存在**（`renderer/workspace/line-comments.ts`、`components/file-tree-v2*.ts` 等）；其结论针对的是当时的 UI 形态。
- **建议**：如果它的设计判断仍指导"应用层 UI / UX"任务书 → 把仍然成立的设计原则摘进 [应用层 UI/UX 任务书](../taskbooks/application-ui-ux-taskbook-2026-09-22.md) 后退役；如果只是历史调研 → 直接退役。
- **不做的代价**：17 条失效引用会让读者按过时结构找代码。

## 5. `reference/module-split-map.md` 与 `reference/repository-guide.md` — 需要更新，不是退役

- **证据**：模块图有 **22 个失效引用**（如 `renderer/workspace/terminal.ts`、`preview-pane.ts`、`Markdown.ts`），仓库指南有 **7 个**（如 `renderer/settings/development-environments.ts`）——大多是重命名后的残留（`.ts`→`.tsx`、目录搬迁）与已删除模块。
- **建议**：由我做一次对齐更新（这是"常驻文档必须描述当前事实"的维护工作），而不是退役；如果你希望优先做别的，我把它排到后面。
- **不做的代价**：新接手者会按图找不到模块；门禁目前不检查失效引用，所以不会自动报警。

## 6. 实时网络检索与安全读取任务书 2026-08-28 — 发布门未闭合

- **证据**：自述"实施中；WB-01～WB-08 完成；WB-09 的真实 Provider 发布门**未能闭合**（当时进程没有 Tavily key，且 DNS 把公开域名解析到保留网段）"。此后的工作集中在缓存与 UI，没有推进它。
- **建议**：若近期要做真实 Provider 发布门 → 保留；若不做 → 按"未完成即未完成"退役（并把"未验证"的边界写进 `web-retrieval-security-acceptance-2026-08-29.md`），避免"实施了但从未在真实网络验证"被误读为已完成。
- **不做的代价**：一个 191KB 的任务书长期挂着，读者分不清哪些已实现、哪些从未真机验证。

## 7. 两份被门禁固定为常驻的任务书 — 是否解除固定

- **证据**：`scripts/check-repository-hygiene.mjs` 的 `required` 列表把 `agent-runtime-continuity-taskbook-2026-07-14.md` 与 `memory-atom-vector-catalog-taskbook-2026-07-17.md` 当作**必须存在**的文档。前者仍有"真实网络故障与外部系统副作用待完成"，后者 826 行里多数状态已是"已完成…"。
- **建议**：前者保留（确有未完成项）；后者如果确认完成 → 把稳定事实并入项目状态与 `reference/`，从 `required` 列表移除并退役。需要你确认，因为改动会动到门禁。
- **不做的代价**：门禁把两份历史文档当作基线，任何退役尝试都会直接失败，长期看会阻碍预算回收。

---

**执行方式**：你逐条选"退役／保留／先更新"，我按选择处理；退役一律先汇总事实再 `git rm`，并在提交信息里写明事实去向与被接续的任务书。
